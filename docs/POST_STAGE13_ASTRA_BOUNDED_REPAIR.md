# Post-Stage-13 Astra One-Shot Audit — Bounded Repair Record

Date: 2026-09-06.

Canonical theory freeze: `SLGPC-THEORY-FREEZE-2026-09-05-v2`.

Target journal: **International Tax and Public Finance**.

A single independent Astra hostile pre-submission audit returned:

`GO WITH MINOR REPAIR`

with **no fatal findings**, no theorem-level novelty collapse, and no decisive ITPF mismatch.

This record documents the bounded manuscript repairs adopted from that audit. No frozen model, equilibrium object, proposition result, canonical parameter, threshold, benchmark result, welfare calculation, robustness result, or verification target was changed.

## 1. Open-neighborhood proof clarification

The audit noted that the rational representation of the unscaled exclusionary welfare gap is singular at `theta=0`, so pointwise positivity on `(0,1]` should not by itself be described as a compact-interval continuity argument.

The global-equilibrium appendix now states explicitly that the scaled gap

`theta^2 Delta W(theta)`

extends continuously to zero and satisfies

`lim_{theta -> 0+} theta^2 Delta W(theta) = 46316976555179 / 8043085428750 > 0`.

It also states explicitly that the determinants of the symmetric and antisymmetric blocks of the full policy first-order-condition Jacobian are root-free on `[0,1]`. These endpoint and Jacobian regularity facts, together with the already strict curvature, feasibility, regime, and welfare-gap margins, complete the written open-neighborhood justification.

This is a proof-presentation repair only. It does not alter the certified canonical global-SPNE result.

## 2. Positive baseline market intercept

The manuscript now states the maintained restriction

`m = a - c > 0`

where the market continuation is introduced and again where the five-region Stage-2 map is used. This makes explicit the domain under which the displayed five-region map excludes a separate both-inactive baseline region.

The canonical witness has `m=2`, so the restriction is strict at the witness and remains satisfied on a sufficiently small primitive neighborhood.

## 3. Positive green-productivity link in the switching theorem

The switching-polynomial representation and Proposition `prop:threshold` now state `mu>0` explicitly. The manuscript separately retains the `mu=0` zero-transmission corollary.

This makes the scope of the strictly positive factor in the switching representation explicit and prevents the zero-productivity-link benchmark from being implicitly folded into the switching theorem.

## 4. Items intentionally left to Stage 14

The Astra audit also noted unfinished submission metadata already identified by Stage 13:

- author / affiliation / corresponding-author metadata;
- Funding statement;
- Competing Interests statement;
- live submission-portal fee check.

These remain Stage-14 submission-QA items and are not part of the theory repair.

## Verdict after bounded repair

`ASTRA MINOR REPAIRS INCORPORATED — PROCEED TO STAGE 14 SUBMISSION QA`
